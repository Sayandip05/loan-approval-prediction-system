# ---- API + Inference (needed for local & Docker deployment) ----
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.6
pydantic==2.5.3
requests==2.31.0

# ML runtime (needed to load model.pkl and predict)
pandas==2.1.4
numpy==1.26.3
scikit-learn==1.3.2
xgboost==2.0.3
joblib==1.3.2
shap==0.44.1

# Frontend
streamlit==1.29.0
plotly==5.18.0

# ---- Notebook only (training in Colab/Jupyter) ----
# These are pre-installed in Colab. Only needed if running locally.
lightgbm==4.1.0
imbalanced-learn==0.11.0
matplotlib==3.8.2
seaborn==0.13.1

# MLflow (optional — for experiment tracking)
mlflow==2.9.2

# Jupyter (only needed locally, Colab has its own)
jupyter==1.0.0
notebook==7.0.6
